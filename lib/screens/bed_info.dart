import 'package:flutter/material.dart';

class BedInfo {
  final String title;
  final int count;
  final String status;
  final Color color;

  BedInfo(this.title, this.count, this.status, this.color);
}
